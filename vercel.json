{
  "rewrites": [{ "source": "./src/api/(.*)", "destination": "/api" }],
  "version": 2,
  "builds": [
    {
      "src": "./src/api/(.*)",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api"
    }
  ]
}
